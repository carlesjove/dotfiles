## Code style rules

- No references to issues or tickets in comments or docstrings — not `(LOC-7253)`, not `# CS-1815`. Describe the behaviour so the comment stands alone. Add one only when I ask.
- Prefer code clear enough that it doesn't need many comments.
- Don't add whitespace to align code.
